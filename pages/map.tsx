import MapSection from '@/components/map/MapSection';
import React, { Fragment } from 'react';

const map = () => {
    return (
        <Fragment>
            <MapSection />
        </Fragment>
    );
};

export default map;
